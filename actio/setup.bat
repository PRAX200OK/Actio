@echo off
REM Setup script for Actio (Windows)
REM Clones the repo, builds the binary, and optionally adds it to PATH.





echo   move "%BINARY_NAME%" "%ProgramFiles%\"  (requires administrative rights)
echo Or move the binary somewhere already on your PATH like:echoecho (You may need to start a new terminal for it to take effect.)echo   setx PATH "%%PATH%%;%cd%" /Mnecho.
necho To make this available from anywhere, add this folder to your PATH: